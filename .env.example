# Frappe Docker v16 环境配置
# 复制此文件为 .env 并根据需要修改

# ============ 站点配置 ============
SITE_NAME=mysite.local
ADMIN_PASSWORD=admin

# ============ 数据库配置 ============
DB_PASSWORD=frappe123
# InnoDB 缓冲池大小 (建议为可用内存的 50-70%)
INNODB_BUFFER_POOL_SIZE=1G

# ============ Redis 配置 ============
REDIS_CACHE_MAXMEMORY=256mb
REDIS_QUEUE_MAXMEMORY=256mb

# ============ Gunicorn 配置 ============
# Worker 数量 (建议: 2 * CPU核心数 + 1)
GUNICORN_WORKERS=4
# 请求超时时间 (秒)
GUNICORN_TIMEOUT=120
HTTP_TIMEOUT=120

# ============ 端口配置 ============
FRAPPE_PORT=8080
SOCKETIO_PORT=9000

# ============ 上传限制 ============
CLIENT_MAX_BODY_SIZE=100m
